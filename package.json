{
  "name": "hanami-hair",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @hanami/web",
    "dev:api": "npm run dev --workspace @hanami/api",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "start:api": "npm run start --workspace @hanami/api"
  },
  "engines": {
    "node": ">=22 <26"
  }
}

